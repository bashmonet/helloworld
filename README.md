# Chomp Counter

A tiny interactive dinosaur game: tap the dinosaur's mouth to make it open and increase a locally saved chomp count.

## Run locally

```bash
npm run dev
```

Then open `http://localhost:4173`.

## Deploy

This is a static site ready for Vercel. Import the GitHub repository in Vercel with the default project settings; no build command is required.
